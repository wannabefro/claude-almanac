---
description: Open the digest web UI (implemented in v0.1 digest module)
argument-hint: "[date]"
allowed-tools: Bash(claude-almanac:*)
---

Execute:

```bash
claude-almanac digest ${ARGUMENTS:-today}
```
